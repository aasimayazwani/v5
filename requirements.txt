# requirements.txt  (revision)

# ── Core LLM / LangChain stack ─────────────────────────
openai>=1.15.0
# match the latest public LangChain family (as of 9 Jul 2025)
langchain==0.3.26
langchain-core==0.3.26
langchain-community==0.3.26
langchain-openai==0.1.6        # first build that targets 0.3.x
langgraph>=0.5.2               # 0.5.x works with LangChain ≥0.3.19

# ── UI ────────────────────────────────────────────────
streamlit>=1.35.0

# ── Database / ORM ───────────────────────────────────
sqlalchemy>=2.0.30
pandas>=2.2.0
psycopg2-binary>=2.9.9         # only if you connect to Postgres

# ── Cloud helpers (optional) ─────────────────────────
boto3>=1.34.0
