# ───── Core LLM stack ────────────────────────────────
openai>=1.15.0
langchain==0.3.26          # only pin the meta-package
langchain-openai==0.1.8    # current release built for 0.3.x
langgraph>=0.5.2

# ───── UI ────────────────────────────────────────────
streamlit>=1.35.0

# ───── Database / ORM ───────────────────────────────
sqlalchemy>=2.0.30
pandas>=2.2.0
psycopg2-binary>=2.9.9      # if you talk to Postgres

# ───── Cloud helpers (optional) ─────────────────────
boto3>=1.34.0
